export default function comparePw(pw1, pw2) {
    if (pw1 !== pw2)
    {return false} 
    else 
    {return true}
}

export default function passwordLength(pw1, pw2) {
    if (pw1.length >= 8 && ) {
        return true
    } 
}
